import { type Application } from 'express';
import { config } from './shared/config/config';

// Landing & version discovery


// Module routes


export function registerRoutes(app: Application): void {
  const prefix = `/api/`;

  //  Root & version index


  // Public
 

  // Authenticated

}
